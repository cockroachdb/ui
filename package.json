{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "scripts": {
    "init": "yarn install && yarn lerna run build",
    "start": "lerna run start --parallel"
  },
  "resolutions": {
    "dot-prop": "^6.0.0"
  },
  "workspaces": ["packages/*"]
}
