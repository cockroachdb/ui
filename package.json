{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.3.0"
  },
  "scripts": {
    "init": "yarn install && yarn lerna run build",
    "start": "lerna run start --parallel"
  },
  "resolutions": {
    "@types/react": "^17.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
