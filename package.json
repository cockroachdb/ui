{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@cockroachlabs/ui-components": "^0.2.12",
    "lerna": "^3.20.2"
  },
  "scripts": {
    "init": "yarn install --pure-lockfile && lerna bootstrap --force-local && lerna link",
    "start": "lerna run start --parallel"
  },
  "resolutions": {
    "dot-prop": "^4.2.1"
  }
}
