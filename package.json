{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "5.6.2"
  },
  "scripts": {
    "init": "yarn install && yarn lerna run build",
    "start": "lerna run start --parallel"
  },
  "resolutions": {},
  "workspaces": [
    "packages/*"
  ]
}
